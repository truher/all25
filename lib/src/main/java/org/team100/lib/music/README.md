# lib.music

Wrapper for the CTRE "MusicTone" feature, which enables each motor in the robot
to play a different note: see `Polyphony` for an example which uses MIDI keyboard
input (treated as joystick buttons).