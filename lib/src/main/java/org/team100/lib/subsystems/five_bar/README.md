# lib.subsystems.five_bar

This package implements kinematics for a five-bar linkage.

A five-bar is a two-dimensional parallel manipulator linkage, similar to a parallel SCARA machine.

The analysis uses these coordinates; a5 is the grounded link.

<img src="pantograph.png" width=600 />
